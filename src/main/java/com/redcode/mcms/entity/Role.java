package com.redcode.mcms.entity;

/**
 * System roles used for role-based access control (RBAC).
 * Each role maps to the allowed functionality shown in the UI.
 */
public enum Role {
    ADMIN,
    MANAGER,
    SALES,
    INVENTORY,
    PROCUREMENT,
    FINANCE,
    HR,
    MARKETING
}
